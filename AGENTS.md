# Repository Instructions

- Do not add binary files to this repository.
- If an asset is required in a binary format (for example `.webp` or `.png`), add a same-name placeholder text file instead that explains it is a placeholder.
